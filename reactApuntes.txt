

#COMPONENTES:hay varios tipos de componentes
    --Funcionales:funcion de js/ES6 que retorna un elemento React(JSX)
        //debe comenzar con una letra minuscula
        //no se pueden utilizar props de elementos hijos en casos de un componente dentro de otros componentes
    --De clase:se usan menos que los componentes funcionales
        class saludo extends React.Component{
            render(){
                return <h1>hola{this.props.name}</h1>;
            }
        }

#ESTADO: conjuntos de propiedades de un "componente" y sus valores actuales

#HOOK: funcion especial que fue creada para administrar los estados de los componentes funcionales y otros aspectos de react(solo se usa en componentes funcionales)

#EVENT LISTENER: funcion js que se ejecuta al activarse un evento

#ELEMENTO: unidades mas pequeñas de React, define lo que se va a ver en la pantalla 
    --<h1>hola mundo{user.name}</h1>

#UN COMPONENTE SE FORMAN DE ELEMENTOS

#REACT DOM: paquete que facilita la actualizacion del DOM en react
 
#AQUI DENTRO ESTARÁ TODO EL PROYECTO <div id="root"></div>

#AGREGAR ELEMENTOS(O COMPONENTES) DENTRO DE OTROS ELEMENTOS
    --ReactDOM.render( elemento , document.getElementById(`root`) )
    --el metodo "render" de "ReactDOM" pide el:
        /elemento(o componente) que se va a agregar dentro de otro 
        /contenedor para el elemento(o componente)
    --¡¡PARA USAR "ReactDOM" hay que importarlo!!



$npx create-react-app mi-app
    --se inicia git tambien con este comando


