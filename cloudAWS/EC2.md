# EC2

> Cada Instancia de Ec2 necesita estar alojada en una red, cada red en la que esta instanciada una instancia de EC2 (una instacia de EC2 es una máquina virtual) tiene su propia IP privaday tambien puede tener una IP pública para hacerla accesible.
